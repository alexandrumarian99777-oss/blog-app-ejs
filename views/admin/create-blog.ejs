<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= blog ? 'Edit Blog' : 'Create Blog' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
  <div class="admin-container">
    <div class="admin-header">
      <h1><%= blog ? '✏️ Edit Blog' : '➕ Create New Blog' %></h1>
      <a href="/admin/dashboard" class="admin-btn admin-btn-secondary">← Back to Dashboard</a>
    </div>

    <% if (error) { %>
      <div class="error-message"><%= error %></div>
    <% } %>

    <div class="form-container">
      <form method="POST" action="<%= blog ? '/admin/edit-blog/' + blog._id : '/admin/create-blog' %>">
        <div class="form-group">
          <label for="title">Title *</label>
          <input type="text" id="title" name="title" value="<%= blog ? blog.title : '' %>" required>
        </div>

        <div class="form-group">
          <label for="excerpt">Excerpt (Short Description) *</label>
          <textarea id="excerpt" name="excerpt" maxlength="200" required><%= blog ? blog.excerpt : '' %></textarea>
          <small style="color: #64748b;">Maximum 200 characters</small>
        </div>

        <div class="form-group">
          <label for="content">Content *</label>
          <textarea id="content" name="content" style="min-height: 300px;" required><%= blog ? blog.content : '' %></textarea>
        </div>

        <div class="form-group">
          <label for="author">Author</label>
          <input type="text" id="author" name="author" value="<%= blog ? blog.author : 'Admin' %>">
        </div>

        <div class="form-group">
          <label for="category">Category</label>
          <input type="text" id="category" name="category" value="<%= blog ? blog.category : 'General' %>">
        </div>

        <div class="form-group">
          <label for="imageUrl">Image URL</label>
          <input type="url" id="imageUrl" name="imageUrl" value="<%= blog ? blog.imageUrl : '' %>" placeholder="https://example.com/image.jpg">
          <small style="color: #64748b;">Leave empty for default image</small>
        </div>

        <div class="form-group">
          <label for="tags">Tags (comma separated)</label>
          <input type="text" id="tags" name="tags" value="<%= blog && blog.tags ? blog.tags.join(', ') : '' %>" placeholder="javascript, web development, tutorial">
        </div>

        <button type="submit" class="admin-btn" style="width: 100%;">
          <%= blog ? '💾 Update Blog' : '✨ Create Blog' %>
        </button>
      </form>
    </div>
  </div>
</body>
</html>